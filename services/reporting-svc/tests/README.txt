# Pytest coverage for this service is intentionally missing — adding it is one
# of the course exercises (see exercises.md, "Write Pytest tests for the
# reporting service"). Suggested starting points:
#
#   * Test app.legacy.format_helpers.format_warranty_label for expired,
#     near-expiry, and far-future dates.
#   * Test the /reports/utilization handler with a mocked assets-svc response.
